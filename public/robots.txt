User-agent: *
Allow: /
Disallow: /api/

# Embed pages are noindex utility iframes — don't spend crawl budget on them.
Disallow: /embed/

# The six Disallow/Allow blocks that used to sit here hid the programmatic
# "template + number" landing pages (/stamp-duty/300000 and friends) from
# crawling. Those pages have now been removed from the site entirely, so the
# rules are gone too — nothing under /salary/, /stamp-duty/ etc. needs blocking,
# and the broad Disallow risked shadowing the real calculator pages.

Sitemap: https://joincalc.com/sitemap.xml
